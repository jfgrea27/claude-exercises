# Workflow

- Be sure to typecheck when you’re done making a series of code changes. This can be done using `just typecheck`. You can run this without asking me.
- Be sure to format when you’re done making a series of code changes. This can be done using `just fmt`. You can run this without asking me.
